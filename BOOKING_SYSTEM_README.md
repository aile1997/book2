# 座位预订系统 (Seat Booking System)

生产级别的座位预订系统，基于 Vue 3 + TypeScript + TailwindCSS 构建。

## 🎯 功能特性

### 核心功能

1. **交互式座位选择**

   - 基于 SVG 的可点击座位地图
   - 实时显示座位状态（可用/已占用/已选中)
   - 支持三个桌子区域（Table A, B, C）
   - 每个桌子支持左右两侧座位布局

2. **日期和时间选择**

   - 多日期选择支持
   - 多时间段选择
   - 清晰的选中状态指示

3. **伙伴邀请系统**

   - 搜索伙伴功能
   - 桌子视图查看伙伴位置
   - 支持添加/移除邀请的伙伴
   - 实时显示已邀请伙伴列表

4. **预订确认**
   - 预订摘要展示
   - 积分消耗显示
   - 成功确认动画

## 📁 项目结构

```
src/
├── components/           # 可复用组件
│   ├── SeatMap.vue      # SVG 座位地图组件
│   ├── SeatSelectionModal.vue    # 座位选择模态框
│   ├── FindPartnerModal.vue      # 查找伙伴模态框
│   └── SuccessModal.vue # 成功确认模态框
├── composables/         # 组合式函数
│   └── useSeats.ts      # 座位管理逻辑
├── pages/               # 页面组件
│   └── BookingPage.vue  # 预订页面主组件
├── types/               # TypeScript 类型定义
│   └── booking.ts       # 预订系统类型
└── assets/              # 静态资源
    └── main.css         # 全局样式
```

## 🛠️ 技术栈

- **Vue 3.5.17** - 使用 Composition API 和 `<script setup>` 语法
- **TypeScript 5** - 完整的类型安全
- **TailwindCSS 3.4.11** - 实用优先的 CSS 框架
- **Vite 7** - 快速的开发服务器和构建工具

## 💻 核心组件说明

### 1. SeatMap.vue

SVG 座位地图组件，负责渲染可交互的座位布局。

**Props:**

- `seats: Seat[]` - 座位数据数组
- `selectedSeat: string | null` - 当前选中的座位 ID
- `showTooltip?: boolean` - 是否显示悬浮提示

**Events:**

- `@select-seat` - 座位被选中时触发

**特性:**

- 使用 SVG 绘制座位，支持精确的样式控制
- 支持三个桌子区域（A, B, C）
- 每个座位支持三种状态：可用、已占用、已选中
- 悬浮时显示选中标记

### 2. FindPartnerModal.vue

查找伙伴模态框，支持搜索和桌子视图两种模式。

**Props:**

- `visible: boolean` - 模态框显示状态
- `selectedPartners: string[]` - 已选中的伙伴列表

**Events:**

- `@update:selectedPartners` - 伙伴选择变更
- `@confirm` - 确认选择

**特性:**

- 搜索模式：实时过滤伙伴列表
- 桌子视图：查看特定桌子的伙伴分布
- 支持在三个桌子间切换
- 高亮搜索匹配文本

### 3. useSeats 组合式函数

封装座位管理逻辑的组合式函数。

**返回值:**

- `seats` - 所有座位数据
- `selectedSeat` - 当前选中的座位
- `getSeatsByTable()` - 根据桌子和位置获取座位
- `selectSeat()` - 选择座位
- `clearSelection()` - 清除选择
- `getSeatColor()` - 获取座位颜色

## 🎨 设计系统

### 颜色规范

```typescript
colors: {
  primary: {
    light: '#E9DFFC',    // 浅紫色
    DEFAULT: '#BE9FF6',  // 紫色
    dark: '#784DC7',     // 深紫色
  },
  gray: {
    dark: '#292929',     // 深灰色
    DEFAULT: '#B9B9B9',  // 灰色
    light: '#EAEAEA',    // 浅灰色
    lighter: '#CCC',     // 更浅灰色
  },
  success: '#38D87B',    // 成功绿色
  warning: '#F6BA13',    // 警告黄色
  cyan: '#51D5FF',       // 青色（积分）
}
```

### 字体规范

- 主字体：Rethink Sans, SF Pro, -apple-system, Roboto
- 字号：12px - 48px
- 字重：400 (regular), 500 (medium), 600 (semibold), 700 (bold)

### 间距规范

- 基础间距单位：4px
- 常用间距：4px, 8px, 12px, 16px, 24px, 32px, 48px

### 圆角规范

- 小圆角：8px
- 中圆角：12px
- 大圆角：16px
- 超大圆角：24px, 32px

## 📱 响应式设计

### 断点

- 移动端：< 640px
- 平板端：640px - 1024px
- 桌面端：> 1024px

### 适配策略

1. **移动端优先**

   - 基础样式针对移动端设计
   - 使用 `@media` 查询增强桌面端体验

2. **触摸优化**

   - 按钮最小点击区域：44px × 44px
   - 移除点击高亮：`-webkit-tap-highlight-color: transparent`

3. **安全区域适配**
   - 支持刘海屏等特殊屏幕
   - 使用 `env(safe-area-inset-*)` 变量

## 🔧 使用方法

### 基础使用

```vue
<script setup lang="ts">
import { useSeats } from '@/composables/useSeats'

const { seats, selectedSeat, selectSeat } = useSeats()
</script>

<template>
  <SeatMap :seats="seats" :selected-seat="selectedSeat" @select-seat="selectSeat" />
</template>
```

### 座位数据结构

```typescript
interface Seat {
  id: string // 座位ID (例如: A1, B2, C3)
  table: 'A' | 'B' | 'C' // 桌子名称
  position: 'left' | 'right' // 左侧或右侧
  index: number // 座位索引（0-5）
  status: SeatStatus // 座位状态
  occupiedBy?: string // 占用者姓名
}

type SeatStatus = 'available' | 'occupied' | 'selected'
```

### 时间段数据结构

```typescript
interface TimeSlot {
  id: string
  date: string // 日期 (例如: 11.20)
  weekday: string // 星期 (例如: Wed.)
  times: TimeOption[]
}

interface TimeOption {
  id: string
  time: string // 时间段 (例如: 09:00 - 12:00)
  selected: boolean
}
```

## 🚀 开发指南

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

### 类型检查

```bash
npm run type-check
```

### 代码规范检查

```bash
npm run lint
```

## 📝 最佳实践

### 1. 组件设计

- 遵循单一职责原则
- 使用 TypeScript 进行类型约束
- Props 使用接口定义
- Events 使用 Emits 类型定义

### 2. 状态管理

- 使用 Composition API 组合式函数
- 相关逻辑封装到单独的 composable
- 避免深层次的 prop drilling

### 3. 样式规范

- 优先使用 TailwindCSS 实用类
- 复杂组件使用 `<style scoped>`
- 全局样式定义在 `main.css`
- 使用 CSS 变量管理主题色

### 4. 性能优化

- 使用 `v-memo` 缓存不变的渲染结果
- 大列表使用虚拟滚动
- 懒加载图片和组件
- 合理使用 `computed` 和 `watch`

## 🎯 待优化项

- [ ] 添加骨架屏加载状态
- [ ] 支持键盘导航
- [ ] 添加单元测试
- [ ] 支持国际化（i18n）
- [ ] 添加深色模式
- [ ] 优化 SEO
- [ ] 添加无障碍支持（ARIA）

## 📄 许可证

MIT License

---

**开发团队** | **最后更新时间**: 2024
